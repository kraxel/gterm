name: build
on: [push,pull_request]
jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: sudo apt-get install -y meson pkg-config libgtk-3-dev libvte-2.91-dev
      - run: meson setup . build-github
      - run: ninja-build -C build-github
